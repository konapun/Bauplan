*(template

  ;; Error: template sigil with no typedef
  ;;*

  ;; Error: bad transition
  ;;*)

  ;;(
  ;; Error: bad position for key-val separator
  *(template1
    {
      key | val | :
    }
  )
  ;;)

  ;; Error: Variable types can't have bodies
  ;;$(variable body)

  ;; Error: Unmatched parens
  ))
)

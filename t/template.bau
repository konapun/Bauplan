;;(
  This is a block comment. It can span multiple lines and gets removed by the lexer.
  Demonstrating another line.
;;)
*(template {key: "value" | key2: -0.12, true, false} ;; this is an inline comment.
  @(lambda
  <<<
    A literal can contain anything *(*&(#)) lambda etc
    and span multiple lines
  >>>
  )
)

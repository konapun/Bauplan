;;(
  Testing beginning phases of parser development
;;)
*(test) ;; good
*(test {}) ;; good
;;*({}) ;; bad

#(lambda
  ;; FIXME: uncommenting this will break the parser
  @(lambda
    &(lambda
      $(lambda)
    )
  )
)
